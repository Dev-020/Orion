# Gemini CLI Backend (Thin Wrapper)

The **Gemini CLI Backend** (`orion_core_geminicli.py`) is a lightweight transport layer that leverages the [Google Gemini CLI](https://github.com/google/gemini-cli) to handle chat management, tool execution, and session persistence.

## Architecture

Unlike other cores that manually manage history and system instructions via the GenAI SDK, the Gemini CLI core acts as a "Thin Wrapper":

1.  **Session Persistence**: Uses the CLI's native `--resume <session_id>` flag. Orion maps its internal session IDs to the CLI's session IDs.
2.  **Persona Management**: Uses a `GEMINI.md` file in the project root. The CLI automatically loads this file as persistent system context.
3.  **Prompt Delivery**: Prompts are sent via a temporary JSON file (Data Envelope) using the `--prompt @filename` flag.
4.  **Output Stream**: Uses the CLI's `-o stream-json` flag to receive structured JSONL events (init, message, tool_use, tool_result, result).

## Key Components

### 1. `GEMINI.md`
This file is automatically generated or updated in the project root on every Core initialization. It combines instructions from:
-   `backends/instructions/Primary_Directive.md`
-   `backends/instructions/master_manifest.json`

### 2. Data Envelope
Orion wraps the user prompt and semantic context into a structured JSON envelope:
```json
{
  "system_notifications": [],
  "user_prompt": "...",
  "vdb_context": "...",
  "auth": {
    "user_id": "...",
    "user_name": "...",
    "session_id": "..."
  },
  "timestamp_utc": "..."
}
```

## Advantages
-   **Reduced Latency**: Caches the CLI executable path at startup to avoid `npm root -g` overhead.
-   **Native Capabilities**: Leverage's the CLI's built-in file handling and tool execution without redundant Python logic.
-   **Simplified Core**: The Python code is reduced to a transport layer, making it easier to maintain and faster to execute.
